coverage:
  precision: 3
  range: 10..100
  round: down

  status:
    project:
      default:
        enabled: yes
        target: 40
        threshold: null
        if_not_found: success
        if_ci_failed: error
    patch:
      enabled: no
